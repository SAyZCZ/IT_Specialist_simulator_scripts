WORKDAY PLANNER
===============

The application was prepared as a compact daily work organizer.
It should open an 860 x 560 desktop window with a task plan, progress summary and interactive controls.

The program currently fails during startup. Run main.py, read the traceback and repair the indicated code.
Do not replace the interface with a simplified window. After the repair, run the current version and confirm that the complete application window appears.
